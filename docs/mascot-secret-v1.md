# Mascot secret clip

The corner − / + / fit controls are removed. Dragging, concept expansion, explicit Recentrar and Regresar remain. The volume control is at the top right and the mascot at the bottom right, with its bubble opening to the left.

Six clicks within a rolling three-second window open the user's pelaogood.mp4 against a full-screen black native dialog. The 15.066667-second H.264/AAC source is remuxed with faststart, without re-encoding, at public/video/pelaogood-v1.mp4. It is mounted and requested only after the trigger; no video is preloaded by the initial page. The burst resets after activation. Ordinary clicks retain the four phrase bubbles.

Opening stops the previous evidence player and pauses graph/mascot movement. The clip uses the shared volume preference, provides a user-gesture play fallback, and closes through Escape, the close button or playback completion. Native dialog provides focus isolation; cleanup releases the video, restores scroll and returns focus. Effect setup restores the source after React development strict-effect cleanup.

Validation: 58 tests, typecheck and build. Browser validation: six-click trigger, unmuted playback, full black background, automatic close after completion, focus returned to mascot, no video element before activation or after close. Source video is unchanged on the user's Desktop.
