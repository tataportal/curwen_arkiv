# Curwen Arkiv — perspective presentation

This revision changes presentation only; the 24 reviewed moments and two-level exploration limit remain unchanged.

- The initial equal-angle ring is replaced by irregular spatial pockets. Concepts that share reviewed evidence are placed in neighbouring pockets.
- Lateral edges are added only for exact shared moment IDs; they represent co-mentions, not a new semantic inference.
- Visual foreground, middle and background planes are independent from retrieval depth. A deterministic pinhole projection with yaw/pitch creates foreshortening; SVG curves and node positions use the same projected coordinates.
- The root remains fixed at the origin. Idle varies gently by visual plane and freezes during interaction/playback and reduced-motion preference. Camera operations remain explicit.
- Contrast, size, sharpness and luminous falloff distinguish planes; hover/focus restores clear text. Background haze stays non-interactive.
- Public naming is Curwen Arkiv, including the page title and other site navigation. The commercial/coverage labels and Ambient control are removed from the public presentation at the user's request. Scope remains recorded in project documentation/checkpoints.

Validation: 53 tests pass, including source-backed lateral edges, irregular positions, fixed root projection, additive expansion and the two-level cap. Typecheck/build pass. Local desktop screenshots checked for all four subjects. No corpus data or timestamps changed.
